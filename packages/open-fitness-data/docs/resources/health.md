# Health

## `client.health.ping()`

GET `/health`

Returns API status, timestamp, and uptime.

## `client.health.db()`

GET `/health/db`

Returns the database health status.

## `client.health.stats()`

GET `/health/stats`

Returns uptime, memory usage, record counts, Node version, and timestamp.
