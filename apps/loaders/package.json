{
  "name": "ofdata-loader",
  "version": "0.1.0",
  "module": "index.ts",
  "bin": {
    "ofdata-loader": "./dist/ofdata-loader"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer": "^9.0.9",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --target=bun --outfile=dist/loader-bundle.js",
    "test": "bun test --pass-with-no-tests",
    "lint": "eslint . && prettier --check .",
    "check-types": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/env-manager": "workspace:*",
    "cmd-ts": "^0.15.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "inquirer": "^13.4.2",
    "tslog": "^4.10.2",
    "xlsx": "^0.18.5"
  }
}
