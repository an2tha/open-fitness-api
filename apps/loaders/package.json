{
  "name": "ofdata-loader",
  "module": "index.ts",
  "bin": {
    "ofdata-loader": "./dist/ofdata-loader"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer": "^9.0.9",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --target=bun",
    "test": "bun test",
    "lint": "eslint . && prettier --check ."
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/env-manager": "workspace:*",
    "cmd-ts": "^0.15.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "inquirer": "^13.4.2",
    "tslog": "^4.10.2",
    "xlsx": "^0.18.5"
  }
}
